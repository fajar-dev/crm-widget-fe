/**
 * Plugins Directory
 *
 * Vue plugins that run before the app is mounted.
 * Plugins are auto-registered by Nuxt in alphabetical order.
 *
 * Use for:
 * - Registering global directives
 * - Setting up third-party libraries
 * - Providing global helpers via provide/inject
 *
 * Suffix with .client.ts or .server.ts for environment-specific plugins.
 *
 * @see https://nuxt.com/docs/guide/directory-structure/plugins
 */
