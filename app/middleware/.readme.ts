/**
 * Middleware Directory
 *
 * Route middleware for authentication and authorization guards.
 * Middleware files are auto-registered by Nuxt.
 *
 * Named middleware: Applied per-page via definePageMeta({ middleware: ['auth'] })
 * Global middleware: Suffix with .global.ts to apply to all routes
 *
 * @see https://nuxt.com/docs/guide/directory-structure/middleware
 */
