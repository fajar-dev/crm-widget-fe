// @ts-check
import withNuxt from './.nuxt/eslint.config.mjs'

export default withNuxt(
  /**
   * ESLint flat config for the CRM Widget Frontend.
   *
   * @nuxt/eslint provides:
   * - Vue 3 rules
   * - TypeScript rules
   * - Nuxt-specific rules
   * - Stylistic rules (configured in nuxt.config.ts)
   *
   * @see https://eslint.nuxt.com/
   */
)
